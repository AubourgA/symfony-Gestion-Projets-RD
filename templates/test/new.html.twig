{% extends 'base.html.twig' %}

{% block title %}Espace Creation{% endblock %}

{% block body %}
    <div class="container">
    
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/">Home</a></li>
                <li class="breadcrumb-item"><a href="{{ path('app_test')}}">Tests</a></li>
                <li class="breadcrumb-item active" aria-current="page">Create</li>
            </ol>
        </nav>


        <h1> Creation d'un nouveau test de mesure</h1>
    
        

         {{form_start(form)}}
            <div class="form-group">
                {{ form_label(form.critere)}}
                {{ form_widget(form.critere)}}
                 <div class="form-error">
                  {{ form_errors(form.critere)}}
                </div>
            </div>

              <div class="form-group">
                {{ form_label(form.pres)}}
                {{ form_widget(form.pres)}}
                <div class="invalid-feedback">
                 {{ form_errors(form.pres)}}
                </div>
            </div>

             <div class="form-group">
                {{ form_label(form.duedate)}}
                {{ form_widget(form.duedate)}}
                 <div class="invalid-feedback">
                {{ form_errors(form.duedate)}}
                </div>
            </div>

               <div class="form-group">
                {{ form_label(form.unit)}}
                {{ form_widget(form.unit)}}
                <div class="invalid-feedback">
                {{ form_errors(form.unit)}}
                </div>
            </div>

               <div class="form-group">
                {{ form_row(form.submit)}}
            </div>


   {{form_end(form)}}

    </div>

{% endblock %}